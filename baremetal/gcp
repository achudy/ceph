### NODE 3 ROOT
useradd -d /home/cephuser -m cephuser
passwd cephuser
echo "cephuser ALL = (root) NOPASSWD:ALL" | tee /etc/sudoers.d/cephuser
chmod 0440 /etc/sudoers.d/cephuser
su cephuser

## NODE 3 cephuser
ssh-keygen
cat .ssh/id_rsa.pub
vi .ssh/authorized_keys
chmod 700 .ssh/authorized_keys
sudo yum install lvm2 -y
sudo vgcreate ceph-block-2 /dev/sdb
sudo lvcreate -l 100%FREE -n osd-2 ceph-block-2
sudo vi /usr/bin/yum #python2
sudo vi /usr/libexec/urlgrabber-ext-down #python2

## NODE 1 ROOT
useradd -d /home/cephuser -m cephuser
passwd cephuser
echo "cephuser ALL = (root) NOPASSWD:ALL" | tee /etc/sudoers.d/cephuser
chmod 0440 /etc/sudoers.d/cephuser
su cephuser

## NODE 1 CEPHUSER
ssh-keygen
cat .ssh/id_rsa.pub 
vi .ssh/authorized_keys
chmod 700 .ssh/authorized_keys 
ssh cephuser@10.186.0.12
ssh cephuser@10.186.0.11
sudo yum install git -y
git clone https://github.com/guv4/ceph.git
cd ceph/container/
git checkout container-wip
vi inventory/hosts 
sudo lsblk
sudo yum install lvm2 -y
sudo vgcreate ceph-block-0 /dev/sdb
sudo lvcreate -l 100%FREE -n osd-0 ceph-block-0
sudo yum  install python-netaddr -y
sudo vi /usr/bin/yum
sudo vi /usr/libexec/urlgrabber-ext-down
sudo yum install ansible -y
ansible-playbook -i inventory/hosts site-container.yml -vv

